{
  "name": "rtpl",
  "version": "0.8.0",
  "description": "CLI tool for resource templates",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "watch": "tsc -b -w"
  },
  "dependencies": {
    "@rtpl/cli": "workspace:*"
  },
  "publishConfig": {
    "directory": "lib"
  },
  "x-wspa": {
    "pkgManifest": {
      "bin": "index.js"
    }
  }
}
